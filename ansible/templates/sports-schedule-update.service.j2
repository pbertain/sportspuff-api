[Unit]
Description=Sports Data Schedule Updater ({{ deployment_env }})
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=ansible
ExecStart=/usr/local/bin/docker-compose -p sports-data-{{ deployment_env }} -f {{ deployment_dir }}/sports-data-service/docker-compose.yml -f {{ deployment_dir }}/sports-data-service/docker-compose.override.yml exec -T sports-service python scripts/update_schedules.py
WorkingDirectory={{ deployment_dir }}/sports-data-service
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
